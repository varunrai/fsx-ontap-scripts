name: Shell_lint

on: 
  push:
    paths:
      - shell/**

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4

    - name: Run ShellCheck
      uses: azohra/shell-linter@latest
      with:
        path: "shell"